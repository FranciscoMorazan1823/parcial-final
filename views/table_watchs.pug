table#tablaLista.table.is-fullwidth
    thead
        tr
            th(title="Posicion") # 
            th(title="id") id
            th(title="nombrebanda") Nombre banda
            th(title="Genero") genero
            th(title="vocalista") integantes
            th(title="Albunes") numero de albunes 
            th(title="Precio") Precio
            th(title="Opciones") Opciones
                
    tbody#bodytable
.level
    .level-left
        button.button.level-item#anterior <--
    .level-rigth
        button.button.level-item#siguiente -->
        